package Craft;

import Hero.Hero;
import Hero.Hit;

public class Wizard extends Craft {

    Wizard(Hero hero) {
        super(hero);
    }

    @Override
    public Hit getHit() {
        return super.getHit();
    }
}
